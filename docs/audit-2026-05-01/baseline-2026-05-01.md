# Baseline health snapshot - 2026-05-01

- Repository: `/Users/apple/Desktop/code/AI/gateway/litellm-rs`
- Branch: `main`
- Baseline commit: `de594c81`
- Rust: `rustc 1.94.0 (4a4ef493e 2026-03-02)`
- Cargo: `cargo 1.94.0 (85eff7c80 2026-01-15)`

## Git state

Command:

```bash
git fetch origin && git status --short --branch
```

Result: pass.

Summary:

```text
## main...origin/main
?? PLAN_AUDIT_EXECUTION.md
?? PLAN_AUDIT_REMEDIATION.md
?? docs/audit-2026-05-01/
?? docs/plan/audit-remediation-complete-plan.md
```

The branch is aligned with `origin/main`. Current local changes are audit/plan documents only.

## Formatting

Command:

```bash
cargo fmt --all -- --check
```

Result: pass.

## Clippy

Command:

```bash
cargo clippy --all-targets --all-features -- -D warnings
```

Result: pass.

Summary:

```text
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1m 57s
```

Note: Cargo emitted an existing file-system-loop warning because `litellm-rs` is a symlink to the repository root.

## Tests

Command:

```bash
cargo test --all-features
```

Result: pass.

Summary:

```text
lib tests: 10361 passed, 0 failed
main tests: 4 passed, 0 failed
tests/lib.rs: 144 passed, 0 failed, 12 ignored
tests/test_connection_pool.rs: 3 passed, 0 failed
doctests: 96 passed, 0 failed, 36 ignored
```

Note: Cargo emitted the same existing file-system-loop warning before the test run.

## Lite feature check

Command:

```bash
cargo check --no-default-features --features lite
```

Result: pass.

Summary:

```text
Finished `dev` profile [unoptimized + debuginfo] target(s) in 30.71s
```

Note: This means the M20 baseline is already green in this checkout.

## Outbound HTTP client baseline

Command:

```bash
rg -n "reqwest::Client::new\(\)" src/ -g '!**/*test*' -g '!**/tests.rs' > /tmp/litellm-rs-reqwest-baseline.txt
wc -l /tmp/litellm-rs-reqwest-baseline.txt
```

Result: pass.

Summary:

```text
50 /tmp/litellm-rs-reqwest-baseline.txt
```

Interpretation: H19 is a multi-batch migration, not a small `~17` site cleanup.

## Large file baseline

Command:

```bash
wc -l src/core/cost/calculator.rs \
      src/core/providers/anthropic/{models,client}.rs \
      src/core/providers/base/sse.rs \
      src/core/providers/openai_like/provider.rs \
      src/server/routes/ai/chat.rs \
      src/core/providers/factory/builder.rs
```

Result: pass.

Summary:

```text
1644 src/core/cost/calculator.rs
1267 src/core/providers/anthropic/models.rs
1151 src/core/providers/anthropic/client.rs
1250 src/core/providers/base/sse.rs
 768 src/core/providers/openai_like/provider.rs
 698 src/server/routes/ai/chat.rs
 620 src/core/providers/factory/builder.rs
7398 total
```

Interpretation: H15 applies primarily to cost, Anthropic, and base SSE files. `openai_like/provider.rs`, `chat.rs`, and `factory/builder.rs` are below the 800-line ceiling in this checkout.
